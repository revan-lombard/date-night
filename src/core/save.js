/**
 * @file core/save.js
 * @responsibility localStorage wrapper that never throws (private mode, quota,
 * disabled storage all degrade to no-op). Persists only "seen intro" and best
 * love score. The game is short enough to need no mid-run save.
 *
 * @phase Implemented in Phase 5.
 */

// TODO(phase 5): safe localStorage get/set.
export {};
