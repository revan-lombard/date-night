/**
 * @file core/audio.js
 * @responsibility Web Audio wrapper: synthesised engine note, UI blips, meter
 * ticks, ambience, plus sample playback for the optional recorded phone call.
 * Owns the master mute toggle and never starts audio before a user gesture.
 *
 * @phase Implemented in Phase 6.
 */

// TODO(phase 6): synth + sample playback, master mute.
export {};
